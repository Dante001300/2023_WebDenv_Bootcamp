// <!-- very important go to sources then select snippets create file index.js -> then clikc ctrl + enter to run ->
// In this lession we'll learn syntax slice it's a way to cut off the string
// for example:    var name = "Dante"
//                 name.slice(0.1) // this means starting at position 0 and stop at but not include position 1. it should result in first letter D 

// for example:    var name = "Dante"
//                 name.slice(0.3) // start at position 0 and stop at but not include position 3 should result in Dan -> -> -->

var char = prompt("type in something");
var char_140 = char.slice(0.140);
alert(char_140);

